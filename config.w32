// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("foa", "for foa support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("foa", "enable foa support", "no");

if (PHP_FOA != "no") {
	EXTENSION("foa", "foa.c");
}

